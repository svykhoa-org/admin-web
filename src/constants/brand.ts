// Single source of truth for the company/brand display name in the admin app.
// Change this one value to rebrand every user-facing brand-name usage.
// Display name only — infrastructure identifiers (email domains, storage bucket,
// DB name, service hostnames) are intentionally NOT derived from it.
export const BRAND_NAME = 'NHAKHOASH'
