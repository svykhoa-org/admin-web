// Single source of truth for the admin app version shown in the sidebar footer.
// Started at 0.0.0; bump this one value manually on every deploy so the sidebar
// reflects exactly which build is live. Current milestone: 0.0.16.
export const APP_VERSION = '0.0.16'
